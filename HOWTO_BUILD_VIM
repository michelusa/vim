#
# using ubuntu 
#

#a required dependency, was not present on my laptop

sudo apt-get install ncurses-dev


#clone, build, install

git clone https://github.com/vim/vim.git
cd vim/src/
make
sudo make install

#note that this does not create a vim upgrade per se. most likely the older vim version is still the default
